{
	"name": "wbs-deploy-setup",
	"description": "Wikibase Suite Deploy Setup",
	"main": "server.js",
	"private": true,
	"type": "module",
	"scripts": {
		"start": "node server.js",
		"build": "npm run typecheck:client && npm run build:client && npm run build:server",
		"build:client": "vite build",
		"build:server": "tsc",
		"typecheck:client": "vue-tsc --noEmit -p tsconfig.client.json"
	},
	"dependencies": {
		"@wikimedia/codex": "^2.5.0",
		"@wikimedia/codex-icons": "^2.5.0",
		"better-sse": "^0.15.1",
		"express": "^5.1.0"
	},
	"devDependencies": {
		"@types/express": "^5.0.3",
		"@types/node": "^24.3.0",
		"@vitejs/plugin-vue": "^6.0.6",
		"typescript": "^5.4.5",
		"vite": "^8.0.9",
		"vue": "^3.5.33",
		"vue-tsc": "^3.2.7"
	}
}
